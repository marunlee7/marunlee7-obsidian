---
cover_url: https://image.aladin.co.kr/product/8025/48/cover150/k102434537_1.jpg
publisher:
category: 한자어휘
title: 사자성어
published:
level: T02
file_role: article_atom
status: planned # 최종 확정: planned. Task 관리 기본값.
priority: medium
process: "" # T01은 process 필드 사용 안 함 (빈 값 유지)
source_name: 사자성어
source_author: "[[이영준]]"
source_type: "book"
references: []
tags: [출처/외부_정보/도서_단행본]
---
# 사자성어
```table-of-contents
style: nestedList
minLevel: 0
maxLevel: 0
includeLinks: true 
debugInConsole: false
```
## 📕 WORKFLOW
```dataview
table without id
link(file.path, regexreplace(file.name, "^(.{2}).*", "$1")) AS "제목", 
작업노트
FROM ""
WHERE 작업노트 != null
where contains(file.tags, "한자어휘/사자성어")
sort file.mtime desc
```

## 📕 INLINE
```dataview
table without id
link(file.path, regexreplace(file.name, "^(.{2}).*", "$1")) AS "제목", 
사색
FROM ""
WHERE 사색 != null
where contains(file.tags, "한자어휘/사자성어")
sort file.mtime desc
```

## 📕 리딩체크 (limit 100+최근 7일간)
```dataview
TABLE WITHOUT ID
theme as "주제",
link(file.path, regexreplace(file.name, "^(.{25}).*", "$1")) AS "제목", 
dateformat(created "yy-MM-dd") as "시작일",
	dateformat(modified, "yy-MM-dd") as "종료일", 
choice(checkbox, "❗", "") AS "❗"
FROM "" 
WHERE contains(file.tags, "한자어휘/사자성어")
and !contains(file.folder, "90-Settings")
AND !contains(file.tags, "moc") 
AND !contains(file.tags, "toc")
AND file.mtime >= date(today) - dur(7 days)
SORT file.mtime DESC
limit 200
```

