---
cover_url: https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTVolAsg8NvoEBeyWjvRnl4s-aUhCyPk1-VIg&s
publisher:
category: 한자어휘
title: 낱말연구
published:
level: T02
file_role: article_atom
status: planned # 최종 확정: planned. Task 관리 기본값.
priority: medium
process: "" # T01은 process 필드 사용 안 함 (빈 값 유지)
source_name: 낱말연구
source_author: "[[이영준]]"
source_type: "book"
references:
  - https://blog.naver.com/adongsu/222967308102
tags: [출처/외부_정보/도서_단행본]
---
# B-한자어휘 낱말연구
```table-of-contents
style: nestedList
minLevel: 0
maxLevel: 0
includeLinks: true 
debugInConsole: false
```
## 📕 작업노트
```dataview
table without id
link(file.path, regexreplace(file.name, "^(.{4}).*", "$1")) as "제목", 
작업노트
FROM ""
WHERE 작업노트 != null
WHERE contains(file.tags, "한자어휘/낱말연구")
sort file.mtime desc
```

## 📕 사색
```dataview
table without id
link(file.path, regexreplace(file.name, "^(.{4}).*", "$1")) AS "제목", 
사색
FROM ""
WHERE 사색 != null
WHERE contains(file.tags, "한자어휘/낱말연구")
sort file.mtime desc
```

## 📕 MOC
```dataview
table without id
file.link as "제목",
choice(checkbox, "❗", "") AS "❗",
dateformat(created, "yyyy-MM-dd") as "시작일",
dateformat(modified, "yyyy-MM-dd") as "종료일"
FROM "" 
WHERE contains(file.tags, "한자어휘/낱말연구")
and !contains(file.folder, "90-Settings")
and !contains(file.folder, "40-아카이브")
AND !contains(file.tags, "moc") 
AND !contains(file.tags, "toc")
sort file.name desc
//sort file.mtime desc
//sort file.name asc
sort file.name desc
```


## 📕 목차



## 📕 소개
