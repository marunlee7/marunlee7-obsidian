---
cover_url: https://image.aladin.co.kr/product/33573/52/cover200/k842939294_1.jpg
publisher:
category: 한자어휘
title: 한자어휘 사자성어
published:
level: T02
file_role: article_atom
status: planned
priority: medium
process: raw
source_name: 한자어휘 사자성어
source_author: "[[이영준]]"
source_type: "book"
action_required: link_to_t04
difficulty:	good
satisfaction: 3
references:
tags: [출처/외부_정보/도서_단행본]
---
# 한자어휘 사자성어

```table-of-contents
style: nestedList
minLevel: 0
maxLevel: 0
includeLinks: true 
debugInConsole: false
```
## 📕 WORKFLOW
```dataview
table without id
link(file.path, regexreplace(file.name, "^(.{0,5}).*", "$1")) AS "제목", 
작업노트
FROM ""
WHERE 작업노트 != null
where contains(file.tags, "한자어휘/사자성어")
sort file.mtime desc
```
## 📕 INLINE
```dataview
table without id
link(file.path, regexreplace(file.name, "^(.{0,5}).*", "$1")) AS "제목", 
사색
FROM ""
WHERE 사색 != null
where contains(file.tags, "한자어휘/사자성어")
sort file.mtime desc
```

## 📕 TOC
```dataview
TABLE WITHOUT ID
link(file.path, regexreplace(file.name, "^(.{10}).*", "$1")) AS "제목", 
choice(checkbox, "❗", "") AS "❗", 
h_sound,
dateformat(created "yy-MM-dd") as "시작일",
	dateformat(modified, "yy-MM-dd") as "종료일"
FROM #한자어휘/사자성어
WHERE !contains(file.folder, "90-Settings")
AND !contains(file.tags, "moc") 
AND !contains(file.tags, "toc")
AND !contains(file.tags, "📚독서, 출처/도서/서명") 
AND file.mtime >= date(today) - dur(7 days)
SORT file.mtime DESC
//limit 100
```
