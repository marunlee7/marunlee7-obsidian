---
weekly_summary: "주간 요약 : 옵시디언 마스터 향하여"
title: 2025-W16
status: planned
priority: medium
process: raw
action_required: link_to_t04
difficulty:	good
satisfaction: 3
achievement:
emotion:
tags: ["Journal/Daily"]
---
# 2025-W16
## 지난 주 반성

-  체력이 공부력이다. 내 몸을 다스려야 한다. 

## 이번 주 계획

-  보다 집중해서 계획적으로 .
### 지난 주에서 가져온 계획

없습니다😀

### 이번 주 계획

## 다음 주 계획

-    옵시디언 맹렬하게

## 이번주 요약

|요일| 날짜 | 내용 |
|---|---|---|
|월|`$=moment("2025-W16", "YYYY-[W]WW").startOf('isoWeek').format("MM-DD")`| ![[2025-04-14#^summary]] |   
|화|`$=moment("2025-W16", "YYYY-[W]WW").startOf('isoWeek').add(1, 'days').format("MM-DD")`| ![[2025-04-15#^summary]] |   
|수|`$=moment("2025-W16", "YYYY-[W]WW").startOf('isoWeek').add(2, 'days').format("MM-DD")`| ![[2025-04-16#^summary]] |  
|목|`$=moment("2025-W16", "YYYY-[W]WW").startOf('isoWeek').add(3, 'days').format("MM-DD")`| ![[2025-04-17#^summary]] |   
|금|`$=moment("2025-W16", "YYYY-[W]WW").startOf('isoWeek').add(4, 'days').format("MM-DD")`| ![[2025-04-18#^summary]] |   
|토|`$=moment("2025-W16", "YYYY-[W]WW").startOf('isoWeek').add(5, 'days').format("MM-DD")`| ![[2025-04-19#^summary]] |   
|일|`$=moment("2025-W16", "YYYY-[W]WW").startOf('isoWeek').add(6, 'days').format("MM-DD")`| ![[2025-04-20#^summary]] |  

## 데일리 리뷰
```dataviewjs
const currentNoteTitle = dv.current().file.name;
const weekNumberMatch = currentNoteTitle.match(/(\d{4}-W\d{2})/);

if (weekNumberMatch) {
    const weekNumber = weekNumberMatch[0];
    const dailyNoteFolder = '"20-에어리어/Journals/Daily"';
    
    dv.pages(dailyNoteFolder)
        .where(page => {
            const pageDate = moment(page.file.name, "YYYY-MM-DD");
            return pageDate.isValid() && pageDate.isoWeek() === moment(weekNumber, "YYYY-[W]WW").isoWeek();
        })
        .forEach(page => {
            const dailyReview = page.리뷰 || "없음";
            dv.paragraph(`**${page.file.name}**: ${dailyReview}`);
        });
} else {
    dv.paragraph("이 노트의 제목에 ISO 주 정보가 포함되지 않았습니다.");
}
```

