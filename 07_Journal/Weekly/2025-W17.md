---
weekly_summary: 
achievement:
emotion:
tags: ["Journal/Daily"]
---
# 2025-W17
## 지난 주 반성

-  작업 진행 기록 철저하게

## 이번 주 계획
### 지난 주에서 가져온 계획

-    옵시디언 맹렬하게

### 이번 주 계획

## 다음 주 계획

-   북노트 정령 및.홈 대쉬보드 

## 이번주 요약

| 요일  | 날짜                                                                                     | 내용                       |     |
| --- | -------------------------------------------------------------------------------------- | ------------------------ | --- |
| 월   | `$=moment("2025-W17", "YYYY-[W]WW").startOf('isoWeek').format("MM-DD")`                | ![[2025-04-21#^summary]] |     |
| 화   | `$=moment("2025-W17", "YYYY-[W]WW").startOf('isoWeek').add(1, 'days').format("MM-DD")` | ![[2025-04-22#^summary]] |     |
| 수   | `$=moment("2025-W17", "YYYY-[W]WW").startOf('isoWeek').add(2, 'days').format("MM-DD")` | ![[2025-04-23#^summary]] |     |
| 목   | `$=moment("2025-W17", "YYYY-[W]WW").startOf('isoWeek').add(3, 'days').format("MM-DD")` | ![[2025-04-24#^summary]] |     |
| 금   | `$=moment("2025-W17", "YYYY-[W]WW").startOf('isoWeek').add(4, 'days').format("MM-DD")` | ![[2025-04-25#^summary]] |     |
| 토   | `$=moment("2025-W17", "YYYY-[W]WW").startOf('isoWeek').add(5, 'days').format("MM-DD")` | ![[2025-04-26#^summary]] |     |
| 일   | `$=moment("2025-W17", "YYYY-[W]WW").startOf('isoWeek').add(6, 'days').format("MM-DD")` | ![[2025-04-27#^summary]] |     |

## 데일리 리뷰
```dataviewjs
const currentNoteTitle = dv.current().file.name;
const weekNumberMatch = currentNoteTitle.match(/(\d{4}-W\d{2})/);

if (weekNumberMatch) {
    const weekNumber = weekNumberMatch[0];
    const dailyNoteFolder = '"20-에어리어/Journals/Daily"';
    
    dv.pages(dailyNoteFolder)
        .where(page => {
            const pageDate = moment(page.file.name, "YYYY-MM-DD");
            return pageDate.isValid() && pageDate.isoWeek() === moment(weekNumber, "YYYY-[W]WW").isoWeek();
        })
        .forEach(page => {
            const dailyReview = page.리뷰 || "없음";
            dv.paragraph(`**${page.file.name}**: ${dailyReview}`);
        });
} else {
    dv.paragraph("이 노트의 제목에 ISO 주 정보가 포함되지 않았습니다.");
}
```

