---
title: T02- Dataview 쿼리 출력 필드 최적화 (화면 폭 문제 해결)
level: T02
file_role: article_atom
parent: 한자어휘 템플릿
status: completed
priority: medium
process: final
source_name: ""
source_author: self
source_type: worknote
action_required: deep_dive
difficulty: good
satisfaction: 5
references: ""
tags:
  - 출처/외부_수집/템플릿
  - 출처/내부_활동/아이디어
  - 개념/옵시디언/템플릿
  - 영역/옵시디언
---
# 📖 T02. T02- Dataview 쿼리 출력 필드 최적화 (화면 폭 문제 해결) 


#### 📅 대화 기록 요약

| **필드**    | **내용**                                                                                                   |
| --------- | -------------------------------------------------------------------------------------------------------- |
| **기록일시**  | 2025-12-03 (수) 오전 7:37                                                                                   |
| **주제**    | T02 Dataview 쿼리 출력 필드 최적화 (화면 폭 문제 해결)                                                                   |
| **핵심 질문** | T02 파일의 화면 폭을 고려하여 Dataview 쿼리에서 출력할 5개 필드(`노트 제목`, `유형`, `중요도`, `진행단계`, `만족도`) 중 **필수 필드만** 선택하여 축소 요청. |

**T02 허브의 핵심 목적**은 **"이 소스(T02)에서 파생된 작업들이 무엇이고, 그 중 가장 중요한 것은 무엇인가"**를 빠르게 파악하는 것입니다.

이 목적에 따라, 5가지 필드 중 **가장 필수적인 3가지 필드**로 축소하는 것을 제안합니다.

### ✂️ T02 Dataview 출력 필드 최적화 (3개 필드 권장)

| **필드 (한국어)** | **필드 (YAML/Dataview)**       | **제거 이유** | **유지 이유 (허브의 필수 정보)**                           |
| ------------ | ---------------------------- | --------- | ----------------------------------------------- |
| **노트 제목**    | `link(file.path, file.name)` | (제거 불가)   | **어떤 노트인지 식별**하는 필수 필드입니다.                      |
| **유형**       | `file_role`                  | (제거 불가)   | **T03, T04, T05 중 어떤 종류의 노트**인지 구분하는 필수 필드입니다.  |
| **중요도**      | `priority`                   | (유지 권장)   | **가장 먼저 처리해야 할 작업**을 표시하는 핵심 필드입니다.             |
| **진행단계**     | `process`                    | **제거 권장** | `file_role`과 `priority`만 있어도 작업 목록 역할 가능.       |
| **만족도**      | `satisfaction`               | **제거 권장** | `priority`에 비해 **즉각적인 행동**을 요구하지 않습니다. (후속 검토용) |

### 🛠️ 최종 쿼리 수정안 (3 필드)

**"노트 제목", "유형", "중요도"** 세 가지만 남기는 것이 가장 깔끔하고 효율적입니다.

코드 스니펫

```
TABLE WITHOUT ID
link(file.path, file.name) AS "노트 제목",
file_role AS "유형",
priority AS "중요도"
FROM ""
WHERE
    contains(parent, this.file.name)
    AND (
        file_role = "concept_dictionary" OR  /* T03 */
        file_role = "personal_analysis" OR   /* T04 */
        file_role = "sentence_collection" OR /* T05 */
        file_role = "person_data"            /* T06 */
    )
    AND file_role != "template"
SORT file_role ASC, file.name ASC
```

이 3개 필드만 사용하시면 화면 폭 문제를 해결하면서 T02의 허브 기능을 완벽하게 수행할 수 있습니다.

## 🗂️ 연결된 하위 노트 목록 (T03/T04/T05/T06)

## 🗂️ 연결된 하위 노트 목록 (T03/T04/T05/T06)

```dataview
TABLE WITHOUT ID
link(file.path, file.name) AS "노트 제목",
file_role AS "유형",
priority AS "중요도",
process AS "진행단계"
FROM ""
WHERE
    contains(parent, this.file.name)
    AND (
        file_role = "concept_dictionary" OR  
        file_role = "personal_analysis" OR   
        file_role = "sentence_collection" OR 
        file_role = "person_data"            
    )
    AND file_role != "template"
SORT file_role ASC, file.name ASC
```
