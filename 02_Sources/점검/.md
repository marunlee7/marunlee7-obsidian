---
<%*
const title = tp.file.title || ""; 
const parts = title.includes('(') ? title.split('(') : [title, ""]; 
const hangul = parts[0].trim(); 
const hanzi = parts[1].replace(')', '').trim(); 

// 파일명 강제 변경 (한자 기반) 
const safeFilename = hanzi .replace(/[^\p{Script=Han}]/gu, "")
.replace(/\s+/g, "") 
.replace(/[()]/g, "");  
await tp.file.rename(safeFilename); 
_%>
aliases:
  - "<% hangul %>(<% hanzi %>)"
  - "<% hanzi %>"
  - "<% hanzi %>(<% hangul %>)"
tags: 한자어휘사전/점검
my_rate: "[[Hard]]"
檢索: https://hanja.dict.naver.com/#/search?query=<% hanzi %>
漢字: "[[<% hanzi %>]]"
訓音: "[[<% hanzi %>|<% hangul %>]]"
解說: ""
dandi: false
---
# <% hanzi %>

<% hangul %>(<% hanzi %>)

<%*
// 괄호와 내용 추출 + 닫힌 괄호 제거
const newFileName = tp.file.title.split("(")[1]?.replace(")", "") || tp.file.title;
await tp.file.rename(newFileName.trim());
%>