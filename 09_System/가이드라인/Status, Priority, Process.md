---
title: Status, Priority, Process
level: T09
file_role: system_config
parent: "[[09_System/가이드라인/09_T-System_Master_Manual]]"
status: planned
priority: medium
process: raw
action_required: link_to_t04
difficulty:	good
satisfaction: 3
---
# 🚦 Status, Priority, Process 필드 최종 표준

본 문서는 T-시스템의 **노트 운영 상태(Status)와 내용 품질(Process)을 추적**하는 관리 필드의 최종 표준을 정의합니다. `09_T-System_Master_Manual`의 간략화된 YAML 테이블보다 **본 문서의 상세 정의를 우선**합니다.

## I. YAML 필드 적용 범위

`status`와 `process` 필드는 노트의 운영(Task)과 내용 품질(Content Quality)을 동시에 추적하기 위해 사용됩니다.

|     YAML 키     | T01 (시리즈) | T02 (원자) | T03 (개념) | T04 (성찰) | T05 (MOC) |
| :------------: | :-------: | :------: | :------: | :------: | :-------: |
|  **`status`**  |   ✅ 적용    |   ✅ 적용   | ❌ (`""`) |   ✅ 적용   | ❌ (`""`)  |
| **`priority`** |   ✅ 적용    |   ✅ 적용   |   ✅ 적용   |   ✅ 적용   | ❌ (`""`)  |
| **`process`**  | ❌ (`""`)  |   ✅ 적용   |   ✅ 적용   |   ✅ 적용   | ❌ (`""`)  |

## II. 필드별 확정 값 (SSoT)

### 1. `status` (운영 상태 / Task)

노트가 현재 사용자 작업 목록에 있는지 여부를 유연하게 관리합니다. **T01, T02, T04**에 적용됩니다.

|     확정 값 (영문 표준)      |          한국어 정의 및 역할           | 기본값 (T01/T02/T04) |
| :-------------------: | :----------------------------: | :---------------: |
|     **`planned`**     |         계획/예정됨 (작업 예정)         |    T02 (발췌 후)     |
|   **`in-progress`**   |        진행 중 (현재 작업 착수)         |    T04 (성찰 시작)    |
| **`waiting/blocked`** | 보류/대기 중 (작업 중단, 우선순위 상향될 수 있음) |         -         |
|    **`completed`**    |       작업 완료 (Task가 마무리됨)       |   T01 (시리즈 완성)    |

### 2. `priority` (중요도)

Dataview 쿼리에서 노트의 중요도 순으로 정렬하는 기준으로 활용됩니다. **T01, T02, T03, T04**에 적용됩니다.

| 확정 값 (영문 표준) |     한국어 정의 및 역할     |            기본값 (T01/T02/T03/T04)             |
| :----------: | :-----------------: | :------------------------------------------: |
|  **`high`**  | 매우 중요, 최우선 처리/복습 필요 | T05 (MOC) (단, T05는 필드 자체를 사용하지 않고 `""`으로 처리) |
| **`medium`** |     중요, 일반적 중요도     |            **T01, T02, T03, T04**            |
|  **`low`**   |  참고용, 중요도가 낮은 콘텐츠   |                      -                       |

### 3. `process` (노트 품질 / 작업 단계)

노트 내용의 품질/작업 단계를 추적합니다. **T02, T03, T04**에 적용됩니다.

| 확정 값 (영문 표준) |          한국어 정의 및 역할           |     기본값 (T02/T04)      |
| :----------: | :----------------------------: | :--------------------: |
|  **`raw`**   |        원본 (외부 발췌, 가공 전)        |      T02 (수집 직후)       |
| **`draft`**  | 초안 (내용 작성 중, 요약은 했지만 확정/검증 필요) |     T04 (성찰 작업 시작)     |
| **`final`**  |   최종 (내용 작성이 완료되어 검증된 완결 상태)   | T03 (개념은 항상 final로 간주) |

---

## III. 문서 진행 상태에 따른 관리 필드 상호작용 (T02 예시)

T02 노트를 예시로 하여, 세 가지 관리 필드가 노트의 생명 주기에 따라 어떻게 상호작용하며 변경되는지를 보여줍니다.

| 단계 | 사용자 행동 | status (운영) | priority (중요도) | process (품질) |
| :---: | :---: | :---: | :---: | :---: |
| **1. 수집** | 외부 자료 발췌 후 T02 노트 생성 | `planned` | `medium` | `raw` |
| **2. 작업 착수** | 요약 및 정리 작업 시작 | `in-progress` | `medium` (유지) | `raw` → `draft` |
| **3. 보류/대기** | 작업을 멈추고 다른 Task를 우선 처리 | `waiting/blocked` | `high` (상향) | `draft` (유지) |
| **4. 완성** | 노트 내용 검토 및 최종 정리 완료 | `completed` | `medium` (하향) | `draft` → `final` |

## 3. T01 ~ T05 템플릿 최종 조정 요약

확정된 SPP 매뉴얼에 따라 T01, T04, T05 템플릿의 YAML 기본값만 재조정합니다.

| **템플릿** | **필드**     | **변경 전 값** | **최종 확정 값**       | **사유**                                           |
| ------- | ---------- | ---------- | ----------------- | ------------------------------------------------ |
| **T01** | `status`   | `active`   | **`planned`**     | T01은 프로젝트 시작 전 `planned`가 논리적 기본값.               |
| **T04** | `status`   | `draft`    | **`in-progress`** | `status`는 운영 Task 관리용이며 `draft`는 `process`에서 사용. |
| **T04** | `process`  | `raw`      | **`draft`**       | T04는 성찰 초안이므로 `process` 기본값은 `draft`가 논리적.       |
| **T05** | `priority` | `high`     | **`""`**          | T05는 관리 필드를 사용하지 않음 (`❌` 적용).                    |

A

시해 주신 원본 테이블의 값(`raw`, `draft`, `final`)과 현재 혼용되어 사용되고 있는 값(`review`, `synthesis`)을 **작업 단계(Process)** 관점에서 비교하여 정리해 보겠습니다.

---

## 📝 `process` 값 비교 및 정의

제미나이가 볼 때, `review`와 `synthesis`는 **원래의 세 가지 작업 단계(`raw`, `draft`, `final`)와는 다른 종류의 작업 목표**를 나타냅니다.

- **`raw`, `draft`, `final`:** **노트의 품질 상태**와 **완성도**를 나타내는 단계적인 지표입니다. (A → B → C)
    
- **`review`, `synthesis`:** **노트에 적용된 특정 행위(Action)** 또는 **목표**를 나타냅니다. (노트를 완성하기 위한 '작업 유형')

|**확정 값 (표준)**|**혼용 값**|**작업 단계 관점 정의 (Process)**|**원래 표준과의 비교**|
|---|---|---|---|
|**`raw`**|해당 없음|원본 데이터의 **최초 수집 상태** (가공 전)|**기초**|
|**`draft`**|**`review`**에 해당 가능|**가공 및 검토가 필요한 초안 상태**. '검토 중'이라는 행위는 '초안' 단계에 포함될 수 있습니다.|**중간**|
|**`final`**|**`synthesis`**에 해당 가능|**내용 작성이 완료되어 확정된 상태**. '종합 및 완성'이라는 행위는 '최종' 단계로 귀결됩니다.|**완성**|

### 💡 `process` 일관성을 위한 권장 사항

시스템의 혼란을 막기 위해, **표준화된 3단계 모델(`raw`, `draft`, `final`)**을 유지하시는 것을 권장하며, 혼용된 값은 다음과 같이 매칭하는 것이 논리적입니다.

- **`review`**가 발견되면 → **`draft`**로 간주하고 **검토가 필요하다**는 의미로 사용합니다.
    
- **`synthesis`**가 발견되면 → **`final`**로 간주하고 **종합 작업이 완료되었다**는 의미로 사용합니다.

| **원래 표준 값** | **적용될 혼용 값** | **의미 (작업 단계)** |
| ----------- | ------------ | -------------- |
| **`raw`**   | (사용하지 않음)    | 최초 수집 상태       |
| **`draft`** | `review`     | 초안 검토 필요 상태    |
| **`final`** | `synthesis`  | 최종 완성 상태       |

## 🗂️ 연결된 하위 노트 목록 (T03/T04)
```dataview
TABLE WITHOUT ID
  link(file.path, file.name) AS "노트 제목",
  file_role AS "유형",
  priority AS "중요도"
FROM ""
WHERE 
  contains(parent, this.file.name) 
  AND (file_role = "dictionary" OR file_role = "personal_analysis") 
  AND file_role != "template"
SORT file_role ASC, file.name ASC
```

