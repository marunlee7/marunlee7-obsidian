---
title: T08 노트의 핵심 내용 무엇을 담아야 하는가
level: T08_Workflow
file_role: tool_config
parent: "[[Obsidian Hub]]"
word_type: ""
status: in-progress
priority: high
process: final
source_name: T08 노트의 핵심 내용 무엇을 담아야 하는가
source_type: web
source_author: Gemini / User
references:
  - "[https://gemini.google.com/share/bb8e3bbf5cb9]"
---
# T08 노트의 핵심 내용 무엇을 담아야 하는가


사용자님의 질문은 T08 노트의 **'내용 정체성'**과 **'구조적 관리'**에 대한 핵심을 짚어주고 계십니다.

T08 문서들은 단순한 지식 저장소(T01-T04)가 아니라, Vault의 **생산 환경과 규칙**을 기록하는 시스템 문서이므로, 내용과 구조에 특별한 원칙이 필요합니다.

---

## 1. T08 노트의 핵심 내용 (무엇을 담아야 하는가?) ⚙️

T08 노트(`file_role: tool_config` 또는 `app_study`)는 앱의 일반 사용법보다는 **사용자님 Vault 환경에 특화된 설정과 워크플로우**를 담아야 합니다.

|**구분**|**T08 노트에 담아야 할 내용**|**예시**|
|---|---|---|
|**Obsidian 설정**|일반 기능이 아닌, **나만의 작업 효율화 규칙**|Dataview 쿼리 핵심 구문, Quickadd 매크로 흐름, Template 폴더 규칙 등|
|**외부 앱 연동**|다른 앱(맥북 툴 등)과의 **데이터 연동 규칙 및 코드**|Bear 노트에서 Obsidian으로 마이그레이션하는 스크립트, Alfred Workflows 설정 등|
|**문제 해결 (Troubleshooting)**|특정 앱 사용 중 발생한 **오류와 해결 과정**|플러그인 충돌 시점과 해결 방법, 특정 파일 경로 문제 해결 로그 등|
|**학습 요약**|새롭게 배운 앱 기능 중 **즉시 적용할 설정**|"Obsidian Properties 사용 시 Tagging 원칙"|

요약하면, T08은 **"이 앱/툴을 T-시스템에 통합하기 위해 내가 결정한 규칙과 코드"**를 기록하는 곳입니다.

---

## 2. `parent` 필드와 MOC 생성 원칙 (구조적 관리)

`parent: [[옵시디언 허브]]`를 입력했으나 파일이 존재하지 않는 상황은 **Bottom-up(상향식) Vault 구축**의 자연스러운 과정입니다.

### 💡 MOC 파일이 없을 때의 처리 원칙

`parent` 필드는 **미래의 연결**을 약속하는 역할을 합니다.

|**상황**|**Dataview 처리**|**사용자 조치**|
|---|---|---|
|**`[[옵시디언 허브]]` 파일이 아직 없을 때**|Dataview 쿼리에서는 `parent` 값이 표시되지만, 링크는 **붉은색**으로 나타납니다.|**아무것도 하지 않아도 됩니다.** T08 노트는 정상적으로 집계됩니다.|
|**MOC 생성이 필요할 때**|T08 노트가 5개 이상 쌓이면, `[[옵시디언 허브]]` 파일을 **직접 생성**합니다.|파일을 생성하면 **붉은색 링크**가 **파란색 링크**로 바뀌고, `[[옵시디언 허브]]` 파일 본문에 모든 T08 노트가 **자동으로 백링크**됩니다.|

T-시스템에서는 **"노트가 쌓일 때까지 상위 MOC를 만들지 않는다"**는 원칙을 적용하여 Vault를 유연하게 관리할 수 있습니다.

따라서 지금은 `parent` 필드에 **`[[옵시디언 허브]]`**를 입력하고, 실제 파일은 나중에 만드는 것이 가장 효율적인 방법입니다.